import type { Metadata } from "next";

export const metadata: Metadata = { title: "MAYA — Supplements" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
